#include <stdio.h>

// Declare the functions (no header needed)
int add(int, int);
int multiply(int, int);

int main() {
    int a = 6, b = 4;
    printf("Addition: %d\n", add(a, b));
    printf("Multiplication: %d\n", multiply(a, b));
    return 0;
}

